[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    -v
    --tb=short
    --strict-markers
markers =
    unit: Pure unit tests — no external dependencies
    costing: CostingEngine tests
    labor: LaborEngine tests
    physics: PhysicsEngine tests
    acp: ACPEngine tests
